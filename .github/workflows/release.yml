name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            arch_suffix: macos-arm64
          - os: macos-latest
            target: x86_64-apple-darwin
            arch_suffix: macos-x64
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            arch_suffix: linux-x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
      - run: cargo build --release --target=${{ matrix.target }}
        env:
          DEVM8_VERSION: ${{ github.ref_name }}
      - run: |
          cp target/${{ matrix.target }}/release/devm8 devm8-${{ matrix.arch_suffix }}
          cp target/${{ matrix.target }}/release/devm8-client devm8-client-${{ matrix.arch_suffix }}
      - uses: actions/upload-artifact@v4
        with:
          name: devm8-${{ matrix.arch_suffix }}
          path: devm8-${{ matrix.arch_suffix }}
      - uses: actions/upload-artifact@v4
        with:
          name: devm8-client-${{ matrix.arch_suffix }}
          path: devm8-client-${{ matrix.arch_suffix }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: artifacts/
      - run: cd artifacts && sha256sum * > ../checksums.txt
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: |
            artifacts/*
            checksums.txt
